import { TestBed } from '@angular/core/testing';

describe('Integration Tests', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({});
  });

  it('should integrate services', () => {
    expect(true).toBe(true);
  });
});
